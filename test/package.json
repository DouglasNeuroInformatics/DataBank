{
  "name": "@databank/test",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "start-server-and-test 'pnpm -w start:test-server' 5500 'echo hello && exit 0'"
  },
  "devDependencies": {
    "cypress": "^14.2.0",
    "start-server-and-test": "^2.0.11"
  }
}
